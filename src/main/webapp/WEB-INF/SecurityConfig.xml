<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security-4.1.xsd">

	<http pattern="/login" security="none" />

	<http auto-config='true'>
		<intercept-url pattern="/index.jsp" access="hasRole ('ROLE_USER')" />
		<intercept-url pattern="/adminHome/**" access="hasRole ('ROLE_ADMIN')" />
		<form-login login-page='/login' default-target-url='/welcome'
			always-use-default-target='true' login-processing-url='/j_spring_security_check'
			authentication-failure-url="/login?error" username-parameter="username"
			password-parameter="password" />
		<logout logout-url='/j_spring_security_logout' logout-success-url="/login?logout" />
		<csrf disabled="true" />

	</http>

	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				authorities-by-username-query="SELECT u.id, r.name FROM user u, USER_ROLE r WHERE  u.admin = r.admin AND u.id = ?"
				users-by-username-query="SELECT id, password, TRUE FROM user WHERE id = ?" />
		</authentication-provider>
	</authentication-manager>



</beans:beans>